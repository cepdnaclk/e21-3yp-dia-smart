import type { Device } from "../types/device";

export const deviceService = {
  // TODO: Integrate device API calls when Milestone 4 backend endpoints are available.
  getDevices: async (): Promise<Device[]> => {
    return [];
  },
};
