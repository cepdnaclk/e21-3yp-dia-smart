server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://diasmart-db.c7myyog8ar0z.eu-north-1.rds.amazonaws.com:5432/diasmart?sslmode=require
    username: postgres
    password: Sanjeevan2002

    jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

logging:
  level:
    "[org.springframework.security]": INFO
    "[org.hibernate.SQL]": DEBUG

app:
  encryption:
    key: ${ENCRYPTION_KEY:prod-secret-key-must-be-32-chars}
  jwt:
    secret: diasmart_super_secure_local_jwt_secret_123456
    expiration-ms: 3600000

mqtt:
  broker:
    ssl: ssl://a36biie00zvpfg-ats.iot.eu-north-1.amazonaws.com:8883

  client:
    id: diasmart-backend

  topic: diasmart/device/telemetry
